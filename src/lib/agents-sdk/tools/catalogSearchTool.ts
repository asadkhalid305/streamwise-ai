// TODO: Define the catalog search tool
// - Use z.object() to define parameters schema: typePreference (enum), genresInclude (array of strings), timeLimitMinutes (number or null)
// - Set tool name and description
// - In execute function, call searchCatalog(query) and return results
export const catalogSearchTool = {} as any;
